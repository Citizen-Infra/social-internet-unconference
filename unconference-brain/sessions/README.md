# Sessions

Each session uses a stable directory:

`<stable-session-id>/`

The session contract will be added when scheduling is implemented. Planned
files are `README.md`, `synthesis.md`, and `links.md`. Session files may hold
public-safe metadata and approved resources, but never attendee identities or
private transcript text.
